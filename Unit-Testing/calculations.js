let calculate = (a, b) => a + b;

let multiply = (a, b) => a * b;

let callName = (nameString) => 'Hello my name is ' + nameString;

let test = (bool) => isNaN(bool);

module.exports = {
    calculate,
    multiply,
    callName,
    test
}